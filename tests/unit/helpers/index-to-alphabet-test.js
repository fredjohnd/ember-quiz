import { indexToAlphabet } from 'ember-quiz/helpers/index-to-alphabet';
import { module, test } from 'qunit';

module('Unit | Helper | index to alphabet');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = indexToAlphabet([42]);
  assert.ok(result);
});
